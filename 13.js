//テンプレートリテラル

const score = 80;
let test = '算数';
const name = 'がずお';

cosole.log(`${name}君の${test}の点数は${score}点です。`);
//がずお君の算数のテストは80点です。

const score = 80;
  let test = '算数';
  const name = 'がずお';
  
  cosole.log(name + '君の' + test + 'の点数は' + score + '点です。');
  //テンプレートリテラルを使わない場合